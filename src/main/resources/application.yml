spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/datajpa
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
      # show_sql: true
        format_sql: true
#  data:
 #   web:
  #    pageable:
   #     default-page-size: 10
        #이것이 컨트롤러에서 페이징할때 디폴트 원래 20개씩이라면 바꾸고싶을 때 글로벌설정방법

logging.level:
  org.hibernate.SQL: debug
# org.hibernate.type: trace